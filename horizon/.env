# API base URL — real app mein actual server URL hoga
# JSONPlaceholder = free mock REST API for testing
VITE_API_BASE_URL=https://jsonplaceholder.typicode.com

# Request timeout — 10 seconds
VITE_API_TIMEOUT=10000

# Mock auth token — real app mein yeh login ke baad milta hai
VITE_MOCK_TOKEN=mock-auth-token-horizon-dev-123
